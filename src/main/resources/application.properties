spring.datasource.url=jdbc:postgresql://localhost:5432/banksim
spring.datasource.username=math
spring.datasource.password=Pp11223344-

#If the value is CREATE then the hibernate first drops the existing tables data and structure,
# then creates new tables and executes the operations on the new tables.

spring.jpa.hibernate.ddl-auto=create
spring.datasource.initialization-mode=always
